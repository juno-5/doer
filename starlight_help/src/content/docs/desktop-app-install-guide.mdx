---
title: Installing the Doer desktop app
---

import {Steps, TabItem, Tabs} from "@astrojs/starlight/components";

import DoerNote from "../../components/DoerNote.astro";
import DoerTip from "../../components/DoerTip.astro";
import AppWillUpdateTip from "../include/_AppWillUpdateTip.mdx";

The Doer desktop app comes with native desktop notifications, support for
multiple Doer accounts, and a dedicated tray icon.

Installing the latest stable release is recommended for most users. For an early
look at the newest features, consider the [beta
releases](#install-a-beta-release).

## Install the latest release

<Tabs>
  <TabItem label="macOS">
    <AppWillUpdateTip />

    #### Disk image *(recommended)*

    <Steps>
      1. Download [Doer for macOS](https://zulip.com/apps/mac).
      1. Open the file, and drag the app into the **Applications** folder.
    </Steps>

    #### Homebrew

    <Steps>
      1. Run the command `brew install --cask doer` from a terminal.
      1. Run Doer from **Applications**.
    </Steps>

    You can run the command `brew upgrade doer` to immediately upgrade the app.
  </TabItem>

  <TabItem label="Windows">
    #### Web installer *(recommended)*

    <AppWillUpdateTip />

    <Steps>
      1. Download and run [Doer for Windows](https://zulip.com/apps/windows).
      1. Run Doer from the **Start** menu.
    </Steps>

    #### Offline installer *(for isolated networks)*

    <DoerNote>
      The app will not update automatically. You can repeat these steps to upgrade
      to future versions.
    </DoerNote>

    <Steps>
      1. Download [doer-x.x.x-x64.msi][latest] for 64-bit desktops
         (common), or [doer-x.x.x-ia32.msi][latest] for 32-bit (rare).
      1. Copy the installer file to the machine you want to install the app
         on, and open it there.
      1. Run Doer from the **Start** menu.
    </Steps>
  </TabItem>

  <TabItem label="Linux">
    #### APT *(Ubuntu or Debian)*

    <DoerTip>
      The app will be updated automatically to future versions when you do a
      regular software update on your system, e.g., with
      `sudo apt update && sudo apt upgrade`.
    </DoerTip>

    <Steps>
      1. Enter the following commands into a terminal:
         ```bash
         sudo apt install curl
         sudo curl -fL -o /etc/apt/trusted.gpg.d/doer-desktop.asc \
             https://download.zulip.com/desktop/apt/doer-desktop.asc
         echo "deb https://download.zulip.com/desktop/apt stable main" | \
             sudo tee /etc/apt/sources.list.d/doer-desktop.list
         sudo apt update
         sudo apt install doer
         ```
         These commands set up the Doer Desktop APT repository and its signing
         key, and then install the Doer client.
      1. Run Doer from your app launcher, or with `doer` from a terminal.
    </Steps>

    #### AppImage *(recommended for all other distros)*

    <AppWillUpdateTip />

    <Steps>
      1. Download [Doer for Linux](https://zulip.com/apps/linux).
      1. Make the file executable, with
         `chmod a+x Doer-x.x.x-x86_64.AppImage` from a terminal (replace
         `x.x.x` with the actual app version of the downloaded file).
      1. Run the file from your app launcher, or from a terminal. This file is the
         Doer app, so no installation is required.
    </Steps>

    #### Snap

    <Steps>
      1. Make sure [snapd](https://docs.snapcraft.io/core/install) is installed.
      1. Execute following command to install Doer:
         ```bash
         sudo snap install doer
         ```
      1. Run Doer from your app launcher, or with `doer` from a terminal.
    </Steps>

    #### Flathub

    <Steps>
      1. Make sure you have [Flatpak](https://flathub.org/setup) installed on your
         system.
      1. Use the following command from the official
         [Flathub page](https://flathub.org/apps/org.zulip.Doer) to install Doer:
         ```bash
         flatpak install flathub org.zulip.Doer
         ```
      1. After the installation is complete, you can run Doer using the following
         command:
         ```bash
         flatpak run org.zulip.Doer
         ```
    </Steps>
  </TabItem>
</Tabs>

## Install a beta release

Get a peek at new features before they're released!

<Tabs>
  <TabItem label="Most systems">
    <AppWillUpdateTip />

    <Steps>
      1. Go to the [Doer releases][release-list] page on GitHub, and find the latest
         version tagged with the “Pre-release” label.
      1. If there's a **Pre-release** that's more recent than the [latest release][latest],
         download the appropriate Doer beta installer or app for your system.
      1. To install and run Doer, refer to the instructions for your operating
         system in the [Install the latest release](#install-the-latest-release)
         section above.
    </Steps>
  </TabItem>

  <TabItem label="Linux with APT">
    <DoerTip>
      The app will be updated automatically to future versions when you do a
      regular software update on your system, e.g., with
      `sudo apt update && sudo apt upgrade`.
    </DoerTip>

    #### You don't have the Doer app installed

    <Steps>
      1. Enter the following commands into a terminal:
         ```bash
         sudo curl -fL -o /etc/apt/trusted.gpg.d/doer-desktop.asc \
             https://download.zulip.com/desktop/apt/doer-desktop.asc
         echo "deb https://download.zulip.com/desktop/apt beta main" | \
             sudo tee /etc/apt/sources.list.d/doer-desktop.list
         sudo apt update
         sudo apt install doer
         ```
         These commands set up the Doer Desktop beta APT repository and its signing
         key, and then install the Doer beta client.
      1. Run Doer from your app launcher, or with `doer` from a terminal.
    </Steps>

    #### You already have the Doer app installed

    <Steps>
      1. Enter the following commands into a terminal:
         ```bash
         sudo sed -i s/stable/beta/ /etc/apt/sources.list.d/doer-desktop.list
         sudo apt update
         sudo apt install doer
         ```
         These commands set up the Doer Desktop beta APT repository, and then
         install the Doer beta client.
      1. Run Doer from your app launcher, or with `doer` from a terminal.
    </Steps>
  </TabItem>
</Tabs>

[latest]: https://github.com/doer/doer-desktop/releases/latest

[release-list]: https://github.com/doer/doer-desktop/releases

## Related articles

* [Connect through a proxy](/help/connect-through-a-proxy)
* [Use a custom certificate](/help/custom-certificates)
* [View Doer version](/help/view-doer-version)
