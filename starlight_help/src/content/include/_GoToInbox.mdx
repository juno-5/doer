import InboxIcon from "~icons/doer-icon/inbox";

1. Click on <InboxIcon /> **Inbox**
   (or <InboxIcon /> if the **views**
   section is collapsed) in the left sidebar,
   or use the <kbd>Shift</kbd> + <kbd>I</kbd> keyboard shortcut.
